-- Personal window rules. Loaded from hyprland.lua, after Omarchy's defaults.

-- HoYoPlay's launcher tiles itself into whatever it lands on; float it instead.
o.window({ title = "^HoYoPlay$" }, { float = true, center = true })
